/*function calculatePercentage(value, percentage){
    let resultado = (value*percentage)/100
    return resultado
}
let valor = 1000 
let porcento = 25
console.log(calculatePercentage(valor,porcento))*/
const calculatePercentage = (value, percentage) => {
    let resultado = (value * percentage) / 100
    return resultado
  }
  let val = 1000
  let per = 25
  console.log(calculatePercentage(val, per))